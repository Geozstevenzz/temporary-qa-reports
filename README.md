# Temporary QA Reports

This public repository hosts short-lived, privacy-reviewed QA evidence reports. GitHub Pages publishes the `docs` directory. A daily workflow removes report directories on their expiry dates and requests a fresh Pages build.

Each report directory must contain an `index.html` and a `report.json` with `published_at` and `expires_at`.
